using UnityEngine;

public class EndTrigger : MonoBehaviour
{/*
    const float WaterHeight = (float)-5.59;
    public Transform player;
    public GameManager gameManager;
    TextMesh gameOver;

    bool flag = true;
    // Update is called once per frame
    void Update()
    {
        if (player.position.y <= WaterHeight && flag)
        {
            flag = false;
            gameManager.EndGame();
        }

    }*/
}
